import withAuthen from "../authen";

const Admin = () => {

    // logic chung 20 dòng 

    return (
        <>
        Trang quản trị đã đăng nhập thành công
        </>
    );
}



export default withAuthen(Admin);